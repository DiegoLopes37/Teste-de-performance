version: '3.9'

services:

  locust:

    build: .

    container_name: orangehrm_locust

    ports:
      - "8089:8089"

    command: >
      locust
      -f locustfile.py
      --host https://opensource-demo.orangehrmlive.com